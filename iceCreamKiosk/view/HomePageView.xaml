<UserControl x:Class="iceCreamKiosk.view.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:iceCreamKiosk.view"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Button Content="Admin Page" Command="{Binding NavigateToAdmin}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75"/>
        <Button Content="Usre Page" Command="{Binding NavigateToUser}" HorizontalAlignment="Left" Margin="200,0,0,0" VerticalAlignment="Top" Width="75"/>
        <UserControl Grid.Row="1" Content="{Binding MainPage}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></UserControl>
    </Grid>
</UserControl>
